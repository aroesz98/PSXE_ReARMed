/* The emulator's profiler reads the DWT cycle counter through the NXP device header; here it is CMSIS. */
#ifndef FSL_DEVICE_REGISTERS_SHIM_H_
#define FSL_DEVICE_REGISTERS_SHIM_H_

#include <soc.h>

#endif
